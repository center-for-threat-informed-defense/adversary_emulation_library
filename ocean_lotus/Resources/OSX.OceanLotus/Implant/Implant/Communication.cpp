#include "Communication.hpp"

std::vector<unsigned char> decryptData(std::vector<unsigned char> data) {
    std::vector<unsigned char> result;
    return result;
}

uint8_t extractCommand(Communication packet) {
    return 0;
}